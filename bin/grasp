#!/usr/bin/env node
const path = require('path');
const grasp = require(path.join(__dirname, '..', 'lib'));

grasp({
  args: process.argv,
  exit: code => { process.exitCode = code; },
  stdin: process.stdin,
  fs: require('fs'),
  callback: console.log,
  error: console.error
});
